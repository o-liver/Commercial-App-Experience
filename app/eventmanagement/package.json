{
    "name": "eventmanagement",
    "version": "0.0.1",
    "description": "A Event Management application.",
    "keywords": [
        "ui5",
        "openui5",
        "sapui5"
    ],
    "main": "webapp/index.html",
    "scripts": {
        "deploy-config": "npx -p @sap/ux-ui5-tooling fiori add deploy-config cf",
        "build:cf": "node ../node_modules/@ui5/cli/bin/ui5.js build preload --clean-dest --config ui5-deploy.yaml --include-task=generateManifestBundle generateCachebusterInfo",
        "preinstall": "cd ../ && npm install",
        "postinstall": "npx shx rm -rf node_modules && echo 'Perform `npm install` only in folder `app/`'"
    },
    "devDependencies": {
        "@sap/ui5-builder-webide-extension": "1.0.x",
        "@ui5/cli": "^2.11.1",
        "@ui5/fs": "^2.0.6",
        "@ui5/logger": "^2.0.1",
        "mbt": "^1.0.15",
        "rimraf": "3.0.2",
        "ui5-task-zipper": "^0.3.1"
    },
    "ui5": {
        "dependencies": [
            "@sap/ui5-builder-webide-extension",
            "ui5-task-zipper",
            "mbt"
        ]
    }
}
